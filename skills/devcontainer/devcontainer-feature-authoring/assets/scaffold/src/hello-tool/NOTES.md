## Usage notes

This is the example feature shipped with the repository scaffold. Replace
it with a real feature (and keep this NOTES.md pattern: the release
workflow appends it to the auto-generated README).

Document here what the generated options table cannot express: supported
base images, interactions with other features, and upgrade caveats.
